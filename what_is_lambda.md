파이썬의 람다(lambda) 함수는 한 줄로 간단한 함수를 만들 때 사용하는 문법이에요.
보통 “익명 함수(이름 없는 함수)”라고 부릅니다.


---

🧩 1. 기본 문법

lambda 매개변수: 표현식

예시:

lambda x: x + 1

➡️ “x를 받아 x+1을 반환하는 함수”

보통 이렇게 변수에 저장해 사용합니다:

add_1 = lambda x: x + 1
print(add_1(5))   # 6


---

🧩 2. 여러 개의 매개변수 사용

lambda x, y: x + y

예시:

add = lambda a, b: a + b
print(add(3, 7))   # 10


---

🧩 3. 조건문도 사용 가능 (한 줄에서 표현식으로만)

lambda x: "짝수" if x % 2 == 0 else "홀수"

예시:

check = lambda x: "짝수" if x % 2 == 0 else "홀수"
print(check(3))  # 홀수


---

🧩 4. 람다가 많이 쓰이는 곳

✔ map() 과 함께

리스트의 모든 값을 처리할 때:

numbers = [1, 2, 3, 4]
result = list(map(lambda x: x * 2, numbers))
print(result)   # [2, 4, 6, 8]


---

✔ filter() 과 함께

조건을 만족하는 요소만 남길 때:

numbers = [1, 2, 3, 4, 5, 6]
evens = list(filter(lambda x: x % 2 == 0, numbers))
print(evens)   # [2, 4, 6]


---

✔ sorted() 의 key에 사용

정렬 기준을 원하는 대로 바꾸기:

items = [("apple", 3), ("banana", 1), ("cherry", 2)]
sorted_items = sorted(items, key=lambda x: x[1])
print(sorted_items)
# [('banana', 1), ('cherry', 2), ('apple', 3)]


---

🧩 5. 람다와 일반 함수의 차이

일반 함수	람다 함수

이름 O	이름 X (익명)
여러 줄 가능	한 줄만 가능
def 사용	lambda 사용
가독성 좋음	간단한 표현에만 적합


예시 비교:

def add(x, y):
    return x + y

add2 = lambda x, y: x + y


---

🧩 6. 언제 람다를 쓰면 좋을까?

✔ 아주 짧고 단순한 연산을 함수로 표현할 때
✔ 다른 함수에 “함수 자체”를 전달할 때 (key=, map(), filter() 등)
❌ 여러 줄의 복잡한 로직에는 사용하면 안 됨 → 가독성 나빠짐


---
