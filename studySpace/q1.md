# code each items.

## 1. session - list
- 1.1. 10칸의 중앙에 "안녕"을 text변수에 넣고, 남은 공간을 공백으로 채움을 출력
- 1.2. 10칸의 중앙에 "안녕"을 text변수에 넣고, 남은 공간을 '-'로 채움을 출력
- 1.3. t=[[1], [1,2], [1,2,3]] 리스트에서, t[-1]을 출력
- 1.4. t=[[1], [1,2], [1,2,3]] 리스트에서, t[-1]을 문자열로 매핑하여 리스트로 출력
- 1.5. "1.4."의 t[-1]을 문자열로 매핑하여 공백없는 문자열로 join하고, length를 출력
- 1.6. t=[[1], [1,2], [1,2,3]] 리스트에 [1,2,3,4]를 추가
- 1.7. p1 = [5, 8], p2 = [3, 6] 일때, 두 점의 manhattan distance를 구하는 코드
- 1.8. permutations, combinations을 사용하기 위한 모듈 import 코드
- 1.9. 'ABC' 중 순서를 고려한(중복O), 모든 2가지 경우의 순열 생성 코드
- 1.10. 'ABC' 중 순서를 고려하지 않은(중복X), 모든 2가지 경우의 순열 생성 코드
## 2. session - sort
- 2.1. l = [[10, 19], [7, 10], [6, 10]]일때, 각 list의 idx[1]기준, 오름차순으로 sorting하는 코드
- 2.2. l = [[10, 19], [7, 10], [6, 10]]일때, 각 list의 idx[1]기준, 내림차순으로 sorting하는 코드
- 2.3. d = {'A':[10, 19], 'B':[7, 10], 'C':[6, 10]} 일때,  `(key, value)` 쌍을 튜플 형태로 반환하는 코드
- 2.4. d = {'A':[10, 19], 'B':[7, 10], 'C':[6, 10]} 일때, 각 k/v의 key기준, 오름차순으로 sorting하여 dict으로 출력하는 코드
- 2.5. d = {'A':[10, 19], 'B':[7, 10], 'C':[6, 10]} 일때, 각 k/v의 key기준, 오름차순으로 sorting하여 list(tuple)로 출력하는 코드
- 2.6. d = {'A':[10, 19], 'B':[7, 10], 'C':[6, 10]} 일때, 각 k/v의 value기준, 오름차순으로 sorting하여 dict으로 출력하는 코드
- 2.7. d = {'A':[10, 19], 'B':[7, 10], 'C':[6, 10]} 일때, 각 k/v의 value의 idx[1]기준, 오름차순으로 sorting하여 dict으로 출력하는 코드
- 2.8. arr = [5, 2, 0, 9, 0, 1, 0, 5, 0, 6]일때, move 0 to end를 위한 for loop 2 개(중첩X)의 each 코드
## 3. session - dict
- 3.1. 빈 dictionary dic을 생성 후, range(3)범위의 정수 k, 문자열 v를 포함하는 dictionary를 출력하는 코드
- 3.2. dic={1:2,2:1,3:1}에서, 각 key에 해당하는 값(value)만 출력하는 코드
- 3.3. ns=[1,1,2,3], 빈 dictionary dic을 생성 후, dic에 ns의 원소를 k로, 원소 갯수를v(없으면 0, 있으면 1을 추가)로 업데이트 형태의 dict를 출력하는 코드
- 3.4. dic={1:2,2:1,3:1}에서, 값(value)이 가장 큰(max) key(숫자)를 찾아 출력하는 코드
- 3.5. Counter를 사용하기 위한 모듈 from XX import XX 코드
- 3.6. ts = [1,3,2,5,4,5,2,3]에서 각 요소의 등장 횟수를 세어 dict로 출력하는 코드
## 4. session - regression
- 4.1. factorial(5)를 regression 함수로 구현한 결과를 출력하는 코드
- 4.2. sum_list([1, 2, 3, 4])를 regression 함수로 구현한 결과를 출력하는 코드
- 4.3. 6번쨰 fibonacci 숫자-fibonacci(6)을 regression 함수로 구현한 결과를 출력하는 코드
- (!) 이진 트리(binary tree) nodes=[1, 2, 3, 4, 5, 6, 7]구조
```
    1

  2   3

 4 5 6 7
```
- 4.4. 이진 트리nodes에서 regression 함수(preorder(n, 0))로 구현 된 전위순회(preorder traversal) 결과를 출력하는 코드
- 4.5. 이진 트리nodes에서 regression 함수(inorder(n, 0))로 구현 된 중위순회(inorder traversal) 결과를 출력하는 코드
- 4.6. 이진 트리nodes에서 regression 함수(postorder(n, 0))로 구현 된 후위순회(postorder traversal) 결과를 출력하는 코드
## 5. session - bin search tree
- 5.1. lst=[5,3,8,4,2,1,7,10]를 이진 탐색 트리(binary search tree)로 구성하는 코드-insert(bst, nd)를 dict 자료형으로 구현한 결과-solution(lst)를 출력하는 코드
- 5.2. 위 5.1.결과 에서 slst=[1,2,5,6]의 각 원소별 존재 여부를 탐색-search(bst,sval)하여 True, False로 표시한 결과-solution(bst, slist)를 출력하는 코드